[project]
name = "python-day-07-async-io"
version = "0.1.0"
description = "Exercises for Python Day 07: Async IO"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
